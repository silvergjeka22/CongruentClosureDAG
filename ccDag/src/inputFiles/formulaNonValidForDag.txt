((~(select(store(a,i,v),i) = ~v) -> ~(select(a,i) != ~v)) | (~(select(a,i))) = car(x))
# (((((((()))))))) this formula is just to test the cnf to dnf transformation when we have all possibilities just take off the comment to make the code works

